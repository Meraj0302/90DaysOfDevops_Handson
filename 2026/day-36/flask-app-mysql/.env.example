# Copy this file to .env and fill in real values before running docker compose.
# .env is gitignored / dockerignored — never commit real credentials.

# --- MySQL ---
MYSQL_ROOT_PASSWORD=root
MYSQL_DB=devops
MYSQL_USER=admin
MYSQL_PASSWORD=admin

# --- App ---
APP_PORT=5000
